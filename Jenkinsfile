pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh 'echo "Build is ready"'
      }
    }
    stage('Tests') {
      steps {
        echo 'Tested successfully'
      }
    }
    stage('Deploy') {
      steps {
        echo 'Ready to deploy'
      }
    }
  }
}